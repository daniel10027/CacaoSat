global:
  scrape_interval: 30s
  external_labels:
    project: cacaosat

scrape_configs:
  - job_name: cacaosat-backend
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ["backend:8000"]

  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
